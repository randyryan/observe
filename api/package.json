{
  "name": "prom4j/openapi-specification-gen",
  "description": "This project is for generating the OpenAPI Specification only. To build client dists, using their projects respectively.",
  "scripts": {
    "postinstall": "npm install --save-dev --location=local ./airtasker-spot-1.9.0-lwan-mod.tgz",
    "spotGenerate": "npx @airtasker/spot generate --contract src/icw-api.ts --generator openapi3 --language yaml --out build/specs/",
    "extractTemplates": "java -jar"
  },
  "devDependencies": {
    "@airtasker/spot": "file:airtasker-spot-1.9.0-lwan-mod.tgz"
  }
}
